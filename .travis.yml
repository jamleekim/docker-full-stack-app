language: generic

sudo: required

services:
  - docker

before_install:
  - docker build -t jamlee/react-test-app -f ./frontend/Dockerfile.dev ./frontend

script:
  - docker run -e CI=true jamlee/react-test-app npm test

after_success:
  - docker build -t jamlee/docker-frontend ./frontend
  - docker build -t jamlee/docker-backend ./backend
  - docker build -t jamlee/docker-nginx ./nginx

  - echo "$DOCKER_HUB_PASSWORD" | docker login -u "$DOCKER_HUB_ID" --password-stdin

  - docker push jamlee/docker-frontend
  - docker push jamlee/docker-backend
  - docker push jamlee/docker-nginx